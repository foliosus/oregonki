!!! 1.1
%html{xmlns: 'http://www.w3.org/1999/xhtml'}
  %head
    %meta{charset: 'utf-8', :'http-equiv' => 'Content-Type'}
    %meta{content: 'IE=edge,chrome=1', :'http-equiv' => 'X-UA-Compatible'}

    %title
      - if current_page.data.meta_title
        = "#{current_page.data.meta_title} | Oregon Ki Society"
      - else
        Oregon Ki Society

    - if current_page.data.meta_description
      %meta{:'http-equiv' => 'Description', content: current_page.data.description}

    = stylesheet_link_tag 'shoji'

    = yield_content :css

  %body{class: page_classes}
    #container
      #wrapper
        #header
          %p= link_to 'Oregon Ki Society (oregonki.org)', '/index.php', title: 'Return to the Oregon Ki Society homepage'

        #primary_content
          %h1= current_page.data.title
          = yield

        #navigation= partial 'layouts/navigation'

        #secondary_content
          = yield_content :secondary_content

        #footer= partial 'layouts/footer'

    = partial 'layouts/google_analytics'

    = yield_content :javascript